import{describe,expect,it,vi}from'vitest';import{parseVoice}from'./voiceParser';
describe('parseVoice',()=>{it('erkennt Todo mit Datum und Uhrzeit',()=>{vi.setSystemTime(new Date('2026-09-20T10:00:00'));const r=parseVoice('Aufgabe Rechnung Müller prüfen, fällig morgen um 14 Uhr');expect(r.kind).toBe('todo');expect(r.dueDate).toBe('2026-09-21');expect(r.dueTime).toBe('14:00')});it('fragt bei normaler Notiz nach Ziel',()=>{const r=parseVoice('Notiz Material bestellen');expect(r.kind).toBeNull();expect(r.needsDestinationChoice).toBe(true)});it('erkennt Pinnwandnotiz',()=>{expect(parseVoice('Pinnwandnotiz Maschine prüfen').kind).toBe('pinboard')});it('erkennt relative Wochen',()=>{vi.setSystemTime(new Date('2026-09-20T10:00:00'));expect(parseVoice('Aufgabe Test fällig in 2 Wochen').dueDate).toBe('2026-10-04')});it('erkennt Mitarbeiter',()=>{expect(parseVoice('Aufgabe für Daniel: Material bestellen').assigneeName).toBe('Daniel')})});
