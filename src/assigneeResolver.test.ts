import{describe,expect,it}from'vitest';import{resolveAssignee}from'./assigneeResolver';const staff=[{id:'1',displayName:'Daniel Adler',active:true},{id:'2',displayName:'Anna Adler',active:true},{id:'3',displayName:'Daniel Müller',active:true},{id:'4',displayName:'Peter Alt',active:false}];describe('resolveAssignee',()=>{it('findet eindeutigen Vollnamen',()=>expect(resolveAssignee('Anna Adler',staff).employee?.id).toBe('2'));it('meldet mehrdeutigen Vornamen',()=>expect(resolveAssignee('Daniel',staff).status).toBe('ambiguous'));it('ignoriert inaktive Mitarbeiter',()=>expect(resolveAssignee('Peter',staff).status).toBe('missing'));it('meldet unbekannte Namen',()=>expect(resolveAssignee('Unbekannt',staff).status).toBe('missing'))});
